
let initialState = {
    friends:[
        {id: 1, name: "Anrew"},
        {id: 2, name: "Anya"},
        {id: 3, name: "Valera"},
    ]
}

const navReducer = ( state = initialState, action ) => {
    return state;
}
export default navReducer